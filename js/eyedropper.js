ASTRA.startEyedropper=()=>{ASTRA.state.eyedropper=true;ASTRA.setTool?.('eyedropper');ASTRA.toast('Click a color on the canvas')};
ASTRA.pickColor=(pointer)=>{const c=ASTRA.state.canvas;if(!c||!ASTRA.state.eyedropper)return;const el=c.lowerCanvasEl,rect=el.getBoundingClientRect(),x=Math.floor((pointer.x/c.getWidth())*el.width),y=Math.floor((pointer.y/c.getHeight())*el.height),ctx=el.getContext('2d'),data=ctx.getImageData(Math.max(0,x),Math.max(0,y),1,1).data,hex='#'+[data[0],data[1],data[2]].map(v=>v.toString(16).padStart(2,'0')).join('').toUpperCase();document.getElementById('pickedColor').value=hex;document.getElementById('pickedColor').textContent=hex;document.getElementById('pickedColorSwatch').style.background=hex;document.getElementById('shapeFill').value=hex;ASTRA.state.eyedropper=false;ASTRA.setTool?.('select')};
