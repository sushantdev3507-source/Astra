ASTRA.maskImage=(shape)=>{const o=ASTRA.active();if(!o||o.type!=='image')return ASTRA.toast('Select an image first');const w=o.getScaledWidth(),h=o.getScaledHeight();o.clipPath=shape==='circle'?new fabric.Ellipse({rx:w/2,ry:h/2,originX:'center',originY:'center'}):new fabric.Rect({width:w,height:h,rx:20,ry:20,originX:'center',originY:'center'});ASTRA.markDirty();ASTRA.refresh()};
