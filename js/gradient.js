ASTRA.applyGradient=()=>{const o=ASTRA.active();if(!o)return ASTRA.toast('Select an object first');const c1=document.getElementById('gradientColor1').value,c2=document.getElementById('gradientColor2').value,type=document.getElementById('gradientType').value,angle=Number(document.getElementById('gradientAngle').value)*Math.PI/180;const w=Math.max(o.getScaledWidth(),1),h=Math.max(o.getScaledHeight(),1);let coords;if(type==='radial')coords={x1:w/2,y1:h/2,r1:0,x2:w/2,y2:h/2,r2:Math.max(w,h)/2};else{const x=Math.cos(angle),y=Math.sin(angle);coords={x1:x<0?w:0,y1:y<0?h:0,x2:x>0?w:0,y2:y>0?h:0}}o.set('fill',new fabric.Gradient({type:type==='radial'?'radial':'linear',coords,colorStops:[{offset:0,color:c1},{offset:1,color:c2}]}));ASTRA.markDirty();ASTRA.refresh()};
