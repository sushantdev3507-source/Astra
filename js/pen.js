ASTRA.setPenMode=()=>{const c=ASTRA.state.canvas;if(!c)return;ASTRA.state.pen={points:[],color:document.getElementById('penColor').value,width:Number(document.getElementById('penWidth').value)};c.isDrawingMode=false;ASTRA.setTool?.('pen');ASTRA.toast('Click points on the canvas, then Finish path')};
ASTRA.finishPen=()=>{const c=ASTRA.state.canvas,p=ASTRA.state.pen;if(!c||!p||p.points.length<2)return ASTRA.toast('Add at least two points');const poly=new fabric.Polyline(p.points,{fill:'transparent',stroke:p.color,strokeWidth:p.width,objectCaching:false,name:'Pen Path'});c.add(poly);c.setActiveObject(poly);c.getObjects().filter(o=>o.name==='Pen guide').forEach(o=>c.remove(o));ASTRA.state.pen=null;ASTRA.setTool?.('select');ASTRA.markDirty();ASTRA.refresh()};
