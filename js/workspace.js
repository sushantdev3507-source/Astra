ASTRA.fitCanvas=()=>{const c=ASTRA.state.canvas,stage=document.getElementById('stage');if(!c||!stage)return;const z=Math.min((stage.clientWidth-10)/c.getWidth(),(stage.clientHeight-10)/c.getHeight());const zoom=Math.max(.2,Math.min(2,z));c.setZoom(zoom);ASTRA.state.zoom=zoom;document.getElementById('zoomValue').textContent=Math.round(zoom*100)+'%';c.requestRenderAll()};
