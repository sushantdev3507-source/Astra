ASTRA.applyFrame=(type)=>{const c=ASTRA.state.canvas,o=ASTRA.active();if(!o)return ASTRA.toast('Select an object first');let frame;if(type==='circle')frame=new fabric.Ellipse({rx:o.getScaledWidth()/2,ry:o.getScaledHeight()/2,originX:'center',originY:'center'});else frame=new fabric.Rect({width:o.getScaledWidth(),height:o.getScaledHeight(),rx:type==='rounded'?18:0,ry:type==='rounded'?18:0,originX:'center',originY:'center'});frame.set({left:o.left,top:o.top,fill:'transparent',stroke:document.getElementById('frameStroke').value,strokeWidth:Number(document.getElementById('frameWidth').value),opacity:Number(document.getElementById('frameOpacity').value)/100,name:'Frame'});c.add(frame);c.sendToBack(frame);ASTRA.markDirty();ASTRA.refresh()};
