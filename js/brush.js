ASTRA.setBrushMode=(on=true)=>{const c=ASTRA.state.canvas;if(!c)return;c.isDrawingMode=on;if(on){c.freeDrawingBrush=new fabric.PencilBrush(c);c.freeDrawingBrush.color=document.getElementById('brushColor').value;c.freeDrawingBrush.width=Number(document.getElementById('brushSize').value);ASTRA.setTool?.('brush')}else{c.isDrawingMode=false;ASTRA.setTool?.('select')}};
